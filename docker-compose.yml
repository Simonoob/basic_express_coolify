services:
  express_app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    volumes:
      - ".:/var/www/html"
    environment:
      APP_URL: ${APP_URL}
      PORT: 3000
